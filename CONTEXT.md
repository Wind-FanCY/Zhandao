# 个人知识库（Zhandao）

一个供个人使用的学习知识库，主题是全栈开发与 agent 开发。内容以外部**材料**为底、以本人**标注**为增量，在其上做检索、问答与面试**预练**。

## Language

**材料**:
从外部获取、经本人判读后决定留存的一份内容；正文是来源原文不可改，标题是本人给的标识可改。
_Avoid_: 笔记, 文章, 文档, 资料
_Code_: `material` · `materials/` · `writeMaterial` · `buildMaterialsIndex` · `searchMaterials`

**标注**:
本人针对某份**材料**写下的个人理解，可在**收录**之后任意时间追加。
_Avoid_: 笔记, 评论, 心得, 批注
_Code_: `writeAnnotation` · `annotations/` · `NewAnnotation`

**收录**:
读过一份候选内容后、判定其值得留存并写入库中的动作。
_Avoid_: 上传, 导入, 保存, 采集
_Code_: `POST /api/inbox/keep` · `appendProcessed` · `decision: kept`

**留档**:
读完一份**材料**后判定无可**标注**、但仍值得留在库中的动作。
_Avoid_: 已读, 跳过, 略过, 存档, 收藏
_Code_: `appendArchived` · `materials-read.jsonl` · `POST /api/materials/:id/archive`

**孤岛**:
没有任何**标注**指向、自己也没发出过**标注**、且未经**留档**的**材料**——即还没从它身上留下任何增量的那些。
_Avoid_: 未读, 待处理, 僵尸材料, 死材料
_Code_: `computePool` · `PoolKind` · `GET /api/materials/pool`

**速记**:
本人当场记下、尚未归属到任何**材料**的一句话；它还不是**标注**。
_Avoid_: 草稿, 便签, 待办, 想法
_Code_: `appendQuickNote` · `QuickNote` · `quicknotes.jsonl`

**归属**:
把一条**速记**挂到某一份**材料**上，从而使它成为一条**标注**的动作。
_Avoid_: 分类, 归类, 打标, 关联, 挂载
_Code_: `POST /api/notes/:id/attach` · `writeAnnotation` · `annotations/` · `appendNoteProcessed`

**索引页**:
一个只含目录与元信息、本身不成为**材料**的页面；其价值在于展开出多份**材料**。
_Avoid_: 目录页, 导航页, 首页
_Code_: `from`（**材料** frontmatter 字段，记录展开它的**索引页**）

**收件箱**:
已被本人标记、但尚未判定是否**收录**的候选内容集合。其中的条目还不是**材料**。
_Avoid_: 待办, 队列, 暂存
_Code_: `readInbox` · `InboxEntry` · `GET /api/inbox` · `InboxFolderNotFound`

**关联**:
由一条**标注**承载、从它指向另一条内容的连线；没有**标注**就没有**关联**。
_Avoid_: 链接, 引用, 相关
_Code_: 尚无实现（计划为**标注** frontmatter 的 `targets` 字段）

**题目**:
由一条**标注**即时生成、答完即弃的一道问题；该条**标注**本身就是它的标准答案。
_Avoid_: 试题, 问题, 卡片
_Code_: 尚无实现

**答题记录**:
本人一次作答的结果，不可再生，用于决定其所考的**标注**何时被再次推送。
_Avoid_: 成绩, 日志, 历史
_Code_: 尚无实现

**预练**:
本人主动打开、就一份**材料**里的**练题**逐道作答并自评会不会的动作。
_Avoid_: 出题, 答题, 刷题, 复习, 测验
_Code_: `listDrills` · `GET /api/materials/:id/drills` · `client/src/Drill.tsx`

**练题**:
从一份**材料**正文里提取出的一个现成问题，答案就在同一份**材料**里；它不是**题目**。
_Avoid_: 题目, 考题, 面试题, 材料题, 卡片
_Code_: `Drill` · `collectCandidateLines` · `extractDrills` · `buildDrills` · `.cache/drills-<材料id>.json`

**练题记录**:
本人对一道**练题**的一次自评结果（会 / 不会），只决定下一轮**预练**是否再出这道题。
_Avoid_: 答题记录, 成绩, 错题本, 掌握度
_Code_: `DrillRecord` · `appendDrillRecord` · `readDrillVerdicts` · `drill-records.jsonl` · `POST /api/drills/record`

## Relationships

- **收件箱**中的条目要么经**收录**成为**材料**，要么被划掉，不存在第三种去向
- 一条**速记**要么被归属到一份**材料**从而成为一条**标注**，要么被丢弃——只有这两个终态
- 当库中无可归属的**材料**时，**速记**停留在待归属状态，系统须提示去**收录**一篇，而非静默地让它留在队列里
- 一次**收录**产生恰好一份**材料**
- 一个**索引页**展开出一到多份**材料**，它自己**不是材料**（无内容，不够格）
- **材料**的 `from` 记录展开它的**索引页**。这是事实而非判断，**不构成关联**，不进图谱
- 一份**材料**有零到多条**标注**
- 一条**标注**属于恰好一份**材料**
- 一条**标注**可指向零到多条其他内容（**材料**或**标注**），每个指向即一条**关联**
- 一份被推送的**材料**有**三个**终态：产出**标注**、经**留档**保留、或被划掉删除。
  这是**刻意**打破「不存在第三种去向」那个形状——**收件箱**与**速记**的输入是外部涌入、清空是常态；
  而**孤岛**是库的存量，靠删除把它清零等于把度量它的尺子拆了
- **留档**不是永久的：推送池按「**孤岛**优先，其次**留档**时间最早」排序，
  所以**留档**过的**材料**会在**孤岛**清空后重新进池——Example dialogue 里「三个月后踩了坑回来写」
  正是这条依赖的场景
- **归属**给出的是那条**必然**的边（恰好一份**材料**），**关联**是**额外**的边（零到多条）；标注因此不是「两份材料之间的线」，而是锚在一份材料上、可向外发线的节点
- 一条**关联**由 agent 提议、本人确认并写下理由后成立；无理由的连线不是**关联**
- 一道**题目**由恰好一条**标注**生成；**标注**即其标准答案
- 一条**答题记录**属于恰好一条**标注**；**题目**不留存，故记录不挂在**题目**上
- 一道**练题**从一份**材料**的正文提取而来；正文不可改，故提取结果是正文的**纯函数**，
  属可丢弃派生物（ADR-0003），住 `.cache/`
- 一道**练题**必须能在它所属**材料**的正文里定位到答案；定位不上的不是**练题**，直接丢弃
  ——这是「提取而非生成」的机械判据，不靠 prompt 维持
- 一条**练题记录**属于恰好一道**练题**，且**只**决定下一轮**预练**出不出这道题；
  它不参与**标注**的重推顺序（ADR-0004 的信号源只有**答题记录**）
- **预练**不改变**孤岛**判据：**预练**过但没写**标注**的**材料**仍是**孤岛**——接触不等于增量
- **预练**可能顺带产出一条**标注**（「我原来想错了」），但作答文本本身不是**标注**
  （那是对原文的复述），故不留存

## Example dialogue

> **Dev:** "我扒了一篇讲 useEffect 的官方文档进来，这算**标注**吗？"
> **本人:** "不算，那是**材料**。等我三个月后踩了坑、回来写一句『依赖里放对象必炸』，那句才是**标注**。"
> **Dev:** "那我看完觉得没价值、没往里放的呢？"
> **本人:** "那就没发生**收录**。库里不该有它——这个判断本身是库的一部分。"

## Flagged ambiguities

- 「**速记**找不到宿主**材料**时怎么办」曾未定，**已解决**：系统提示去**收录**一篇，把无宿主的
  **速记**转化成一个**收录**任务。不允许静默停在队列里——那正是 ADR-0009 点名的堆积。
  也不放宽 Q10 的「**标注**必须有宿主」：实测那条速记（undici 不读 http_proxy）本身并不包含
  「这是 Node 内置 fetch 的行为、修法是 EnvHttpProxyAgent」这些信息，脱离来源就查不回来。
  实测触发点：2026-09-16，库中 `undici` / `http_proxy` / `代理` 各 0 命中。

- "知识点"曾被使用但**已解决**：它不是本项目中的实体，仅是口语词。见 ADR-0001。
  **机械检查**：`grep -rniE "\btag\b|\btopic\b|\bcategory\b|知识点" server/src | grep -v test` 应无输出。2026-09-16 基线：0 命中。
- **关联**的两端曾未定，**已解决**：两端可以是**材料**或**标注**，但每条**关联**必须由一条**标注**承载——字面相关由检索覆盖，**关联**只记录检索找不到的那类联系，而那类联系必须有一句理由才说得清。
- 「**材料**是否可修改」曾未定，**已解决**：正文与标题分属两类。
  **正文不可改**——它进检索、进出题，改了等于伪造引文，你会引用到原文里不存在的话。
  **标题可改**——它唯一的用途是让你在列表里认出这条是什么，来源的 `<title>` 只是默认候选值。
  依据是「界面必须支持浏览」这条约束：召回靠认出来，认不出来读路径就断了，而读路径断了
  就是这个库的死法。实测触发点：WHATWG 的 SSE 页抽出的标题是「HTML Standard, Edition for
  Web Developers」，在列表里无法辨识。
- 「实践中直接产生、无宿主**材料**的洞察」曾未定，**已解决**：不允许 `material` 为空的**标注**。
  这类洞察须先**收录**一篇能支撑它的**材料**，再把**标注**挂上去。理由是脱离来源的洞察在数月后
  无法核查（是哪个版本、默认值多少、这个 API 属于谁），而寻找来源的动作本身就是一次验证。
  已知代价：给「记一条洞察」增加了摩擦，而摩擦是功能优先级第 2 条明令要消灭的。
- "题目"曾未定，**已解决**：即时生成、答完即弃，可缓存但不是真相源；留存的是**答题记录**，且挂在**标注**上而非**题目**上——因为需要调度的信号是"这条理解没拿住"，不是"这道题没答对"。

- 「**归属**时那句话可否编辑」曾未定，**已解决**：可编辑，且**标注**文件的正文是真相源。
  依据是**材料**已划好的界线：正文不可改是因为「改了等于伪造引文」，而**标注**整句都是本人写的、不含引文，那条禁令的理由在此不成立；且**题目**要拿它当标准答案。
  `quicknotes.jsonl` 里那行不改动：它记的是「某时刻捕获了什么」这个事实，不是**速记**的当前状态，与 `processed.jsonl` 和 `materials/` 的关系一致。

- 「一次**收录**能否覆盖整个系列」曾未定，**已解决**：可以。**判读**允许在**系列级别**发生——
  本人读过、信任一个系列的作者与质量，即可一次性**收录**该**索引页**展开出的全部**材料**，
  不要求逐篇读完再决定。
  实测触发点：2026-09-16，`xiaolincoding.com/network/` 展开 41 篇，逐篇过闸是数小时阅读量；
  而「库太小」已被实测证明是当时检索失效的根因——同一条**速记**的正确宿主在 6 篇语料里排第 5、
  在 47 篇语料里排第 1，**什么都没改**。
  **已知代价**：库里会出现本人没读过的**材料**，孤岛数随之暴增（ADR-0010 已预告：
  收 41 篇、标注 3 篇 = 38 个孤岛）。**这不是指标坏了，是真相**——那 38 篇确实没被消化。

- 「本人自著的内容、以及跨多份**材料**的综合该算什么」曾未定，**已解决：它是一条标注，不是材料。**
  **材料**按定义是「从外部获取」的，自著内容不够格；而「正文不可改」这条规则的理由是
  「改了等于伪造引文」，对自著内容空转。放进**材料**槽还会被孤岛检测算成「存了没消化」。
  **跨材料的综合用现有模型承载，不需要新实体**：归属到它最主要依据的那份**材料**，
  其余几份用**关联**指向，而**关联**的理由就是该条**标注**的正文本身（ADR-0001 已定）。
  这同时补上了 ADR-0010 Consequences 里那个洞——综合的宿主不必是**索引页**，
  可以是它最主要依据的**那一节**；41 节的系列不需要挂 41 条边。
  **孤岛检测因此更准**：被**关联**指向的**材料**不再是孤岛，而那正是事实。
  Q10「**标注**必须有宿主」完整保留：挑一个主宿主仍然逼你回答「这主要是关于哪篇的」，
  而「寻找来源的动作本身就是一次验证」。
  **已知代价**：这条路逼着把**关联**提前。当前决定是综合先只落单宿主、`targets` 留空，
  等真写出第一篇综合再决定关联怎么做（2026-09-17）。

- 「**标注**只有个位数时推送什么」曾未定，**已解决**：冷启动期推一篇未消化的**材料**（读），
  不是**题目**（考）。依据是功能优先级 #3 的原话——「出题 —— 否则没人回来读」：
  **出题不是目的，让本人回来才是**，出题只是**标注**够多之后最有效的回来方式。
  这不违反 ADR-0004 的「不再引入额外机制」：那句的作用域是**复习调度**（哪条**标注**何时重推），
  而推一篇没读的**材料**不碰**答题记录**、不排任何**标注**，两者不在同一作用域。
  **配套约束**：推送只给一篇，**不报总数、不给进度条**——每天提醒本人欠着几十篇会让人不想打开它，
  而「任何让收录或标注变麻烦的设计都在削弱这个项目」；且池子里没有合适的就**不推**，
  「今天没有」是合法输出，硬推一条昨天刚判断过的**材料**等于给一个已经答过的问题。
  **已知代价**：**留档**是「什么都不写」的唯一出口，一定会被滥用（累的时候一路点留档最省力），
  那样数字好看而库还是没消化。目前没有机械办法区分「认真读完觉得没可写」和「不想写」。

- 「**题目**能不能由**材料**生成」曾未定，**已解决：不能，题目仍只由标注生成。**
  闭环里**题目**的作用是揭示**本人自己的理解**衰减了；面试彩排的作用是排练将被问到的问题。
  两者答错的含义不同——前者说明忘了自己的洞察，后者只说明没记住别人的文章，
  而 ADR-0004 定的调度信号是「这条理解没拿住」，**材料**题产生的**答题记录**对它是噪音。
  **「面试前预练」因此是一条独立需求，不是题目**：那几篇面试题**材料**里现成的问题属于
  「材料内容」，想刷它们需要的是把问题列出来的阅读视图——**那是提取，不是生成**，答案就在原文里。
  这条需求已于 2026-09-18 立为**预练**并定完形状，见下一条；
  **当时随手写下的「它要等标注攒够」是错的**——那是把优先级写成了依赖。

- 「**预练**是什么、记下什么」曾未定，**已解决（2026-09-18，见 ADR-0011）**。
  **它一条标注都不需要**：原料是**材料**正文里现成的问题。实测 49 篇库——
  问句型小标题 91 条、加粗问句行 7 行，另有 29 篇的标题本身就是问句。定下来的形状：
  **①** 产物是**练题记录**（会 / 不会），**不是标注**。把原文的正确答案写下来只是复述，
  属**标注** `_Avoid_` 里点名的「笔记」，进 `annotations/` 会稀释库里唯一有价值的那一层。
  本人原话：「我写出来正确答案并不是标注，只是证明我会这个问题了」——**这一条是本人纠正我的**，
  我原先建议让预练产出**标注**。
  **②** 记录挂在**练题**上而不是**材料**上：`3.1 HTTP 常见面试题` 有 19 道、`4.1` 有 37 道，
  挂材料级等于为了找出没答对的 3 道要重跑 37 道。地址用「材料 id + 正文锚点」，两者都从不可改的正文来。
  **③** **预练**不进推送、不碰推送池，靠本人主动打开；复习 = 只出上次「不会」的
  （一个集合差，无间隔、无常数，故不违反 ADR-0004）。
  **④** **必须先写下答案才能揭晓原文，长度不限。** 不作答的「我会」只是一种感觉，
  而感觉正是要被测的那个东西（本人原话：「可是我不做答我就不知道自己会不会」）；
  但强制完整作答会把 45 道题变成数小时打字，撞上 ADR-0009 的摩擦约束。
  **不设长度阈值**——速扫一行、想练表达一段，由本人按题决定，与「都不合适不设自动阈值」同理。
  **⑤** 作答文本**不留存**，只留会/不会那一位。
  **已知代价**：自评没有外部校准，可以骗自己，唯一防线是「必须先写下来」；
  不留作答故「上次我是怎么答的」查不回来；且**预练**完全依赖本人想起来要练，
  而「你想不起来的东西不会去搜」——所以它是考前工具，不承担「让库不死」那份责任。
