# 数据根路径——材料与标注的存放处（ADR-0005：与代码仓库分离）
# 留空则用代码仓库的兄弟目录 ../data
ZHANDAO_DATA_DIR=

# 模型服务商：DeepSeek（OpenAI 兼容接口）
# 只有 server/src/model/ 下的代码用得到它，其余命令（收录、检索、评估）不需要
DEEPSEEK_API_KEY=

# Chrome 书签文件。留空则用 macOS 默认 profile 的路径。
# 若你用的不是默认 profile（比如 Profile 1），在这里覆盖。
CHROME_BOOKMARKS_PATH=
