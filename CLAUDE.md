# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目状态

决策记录：`CONTEXT.md`（术语表）与 `docs/adr/`（8 条 ADR）。代码在 `server/`，
已完成收录链路前两步：读 Chrome 书签收件箱、抓取并抽取正文。

目录布局：`Zhandao/` 是容器（非仓库），其下 `code/`（本仓库）与 `data/`（材料与标注，独立私有仓库）平级——见 ADR-0005。

`Zhandao/CLAUDE.md` 是指向 `code/CLAUDE.md` 的**软链接**（同一 inode），这样在容器目录开会话也能加载到约束。
内容只有一份、受 `code/` 版本控制。链接本身不受版本控制，坏了就 `ln -sf code/CLAUDE.md CLAUDE.md` 重建。

## 术语表要维护 `_Code_` 行

`CONTEXT.md` 里每个术语都带一行 `_Code_`，记它在代码里的名字（`材料 → material / materials/ / writeMaterial`）。
**原因**：术语是中文、标识符是英文，两者之间本来没有任何机械可验证的联系——
`glossary_code_consistency.py` 会把所有术语报成 dead glossary，等于永久无效。

由此得到的具体好处是**能记录刻意不存在的东西**。ADR-0001 禁止引入 topic / tag 实体，
这条约束原先只靠"有人读了本文件"来维持；现在 `CONTEXT.md` 里写着可执行的检查：

```bash
grep -rniE "\btag\b|\btopic\b|\bcategory\b|知识点" server/src | grep -v test   # 应无输出
```

**重构改了标识符名字，就要同步这一行。** 忘了也没关系——一致性脚本会把它报成 dead，那就是提醒。

动手写任何代码前先读这两处。下面列出的约束全部来自它们，且**大多与常规做法相反**——照直觉写会违反其中好几条。

## 这是什么

一个本地运行、按需启动的个人知识库，主题是全栈开发与 agent 开发。核心是一个闭环：

```
收录 → 标注 → 出题 → 推送 → 答题 → 暴露弱点 → 回去补标注
        检索贯穿全程
```

价值全部集中在**标注**上，不在**材料**上。材料是网上搜得到的东西，存一份不产生增量；标注是本人写的、搜不到的东西。**任何让收录或标注变麻烦的设计都在削弱这个项目。**

## 界面必须支持浏览，不能只有搜索

召回有三种强度：主动搜索最弱（**你想不起来的东西不会去搜**），系统推送居中，被迫调用最强。
本项目的核心失效模式是"没有读路径 → 库会死"，所以界面必须提供**可浏览的列表**——
哪怕只是标题列表。认出来（recognition）比想起来（recall）容易得多，这是搜索框给不了的。

推论：任何"只提供搜索入口"的设计（纯 CLI、只有一个搜索框的首页）都在削弱读路径，不要采用。

**另一条推论，实测得来**：**做决定所需的控件必须在默认视图里，只有「想深入看看」的内容才藏在展开后面。**
第一次真实过闸时六个标题一个都没改——不是懒，是标题输入框被放在「展开」面板里，本人根本没看见。
原设计正好反了：**必须设置**的标题被藏起来，而**可选**的正文预览反而是展开的理由。
可选步骤一旦需要额外点击，在批量流程里就等于不存在。

顺带一条 git 配置：两个仓库都设了 `core.quotepath false`。默认配置会把中文文件名转义成
`\343\200\220…`，而本项目所有文件名都是中文，不设这个 `git log` / `git status` 基本不可读。

## 术语是有约束力的

`CONTEXT.md` 定义了 7 个术语，它们不是同义词。最容易搞错的三处：

- **材料**是导入的原始内容，**标注**是本人写下的理解。两者不是一回事，不要统称"笔记"。
- **收件箱**里的条目**还不是材料**——它还没过"值得留吗"这道闸。
- **题目**不留存，**答题记录**留存，且挂在**标注**上而非题目上。

## 硬约束

每条都有 ADR 支撑。要改动的话先读对应 ADR，别直接推翻。

| 约束 | 出处 |
|---|---|
| 不要引入「知识点」/ topic / tag 实体或表。分类从关联图算出，不预先存 | ADR-0001 |
| 关联必须由一条**标注**承载。不要创建没有理由的"相关"边 | ADR-0001 |
| 不要做 bookmarklet 或浏览器扩展。收录是异步的，走浏览器书签收件箱 | ADR-0002 |
| 不要部署到公网。本地、按需启动 | ADR-0002 |
| Markdown 文件是真相源。SQLite 里的一切必须可从文件重建，且可随时删库 | ADR-0003 |
| 不要写数据库迁移脚本。schema 变了就删库重建 | ADR-0003 |
| 复习调度依据答题表现。不要实现艾宾浩斯 / SM-2 之类的通用间隔算法 | ADR-0004 |
| 定时触发用 launchd，不要起常驻进程 | ADR-0004 |
| **不要写 Chrome 的书签文件**（有 checksum、在云端同步、被 Chrome 进程持有）。已处理条目靠 `data/` 里的清单过滤 | ADR-0008 |

## 技术路线

方向已定，尚未落地。

- **语言**：TypeScript。**前后端分离**：前端 Vite + React，后端 Express（ADR-0007）。
  一体化框架（Next.js / SvelteKit）已被明确否决，不要提议迁移——先读 ADR-0007。
  必须用单条 `npm run dev` 同时拉起前后端，否则违反 ADR-0002 的启动摩擦约束。
- **模型调用**：`server/src/model/` 是**唯一**知道用哪家服务商的地方。
  现在是 **DeepSeek**（`deepseek-flash`，走 OpenAI 兼容接口 `https://api.deepseek.com`）。
  **为什么只有一家却单独关一个目录**——这条已被实战检验过一次：原本选的是 Claude，
  但学校组织账号的 API key 未绑定 workspace，调任何接口都返回 400
  （要求 `anthropic-workspace-id` 头），而本人无权新建 key。换成 DeepSeek 的
  **实际改动范围就是这一个文件**，调用方一行未动。
  **DeepSeek 与 Anthropic 的三处实质差异**（依官方文档，别照搬 OpenAI 的通用写法）：
  ① OpenAI 兼容接口 + `baseURL`；② **没有严格 JSON Schema**，只有
  `response_format: {type:"json_object"}`，schema 校验得自己用 zod 兜；
  ③ 文档明示「API 可能偶尔返回空内容」——已按此重试一次，两次都空就返回空数组，
  不让关键词缺失拖垮整条收录流程。
  **刻意没有造 `interface ModelProvider`**：目前只有一个调用点，基于一个调用点设计的
  接口形状大概率要重做（将来的查询改写要不要流式？出题要不要 tool use？都还不知道）。
  等调用点长到三个以上、能看出共同形状时再抽。
  凭证放 `code/.env` 的 `DEEPSEEK_API_KEY`（`.env` 已被 gitignore，而本仓库是公开的）。
- **Agent**：**自己写 agent 循环**。不要引入 DeepSeek Harness、LangChain 之类的 harness/框架——本项目的目的之一就是学 agent 怎么工作，把循环抽象掉就学不到了。模型本身随意，DeepSeek 可用。
- **RAG 分四阶段推进，每阶段对基线量化**：
  1. BM25 检索 → 生成，拿到基线 —— **已完成**。`npm run eval` 是长期存在的尺子。
     官方基线（2026-09-16，10 条评估集 / 6 篇材料，`k1=1.5` `b=0.75`，整篇为检索单位）：

     ```
     整体        recall@1=60%   recall@3= 70%   MRR=0.710
     含中文材料   recall@1=86%   recall@3=100%   MRR=0.929
     纯英文材料   recall@1= 0%   recall@3=  0%   MRR=0.200   ← 连前三都进不去
     ```

     **已做过、不要重复的消融实验**：剔除「图解网络」那篇无内容的目录页，
     recall@1 不变（60%），MRR 仅 0.710→0.725（n=10 下是噪音）。
     所以「无内容材料拖累检索」这条说法**没有证据**；要不要剔除它，
     理由只能是「它永远答不了任何问题」（CONTEXT.md 的定义问题），不能是检索指标。
  2. 建评估集（约 20 个已知答案的问题），测 recall@5
  3. **索引时翻译 —— 已完成，有效。** 收录纯英文材料时用模型生成中文检索关键词写进
     frontmatter 的 `keywords_zh`，并一并进索引。`npm run keywords -- --write` 回填。

     ```
                 改动前                      改动后
     整体        recall@1=60% @3= 70%        recall@1=80% @3=100%   MRR 0.710→0.883
     含中文材料   recall@1=86% @3=100%        recall@1=86% @3=100%   不变（对照组）
     纯英文材料   recall@1= 0% @3=  0%        recall@1=67% @3=100%   MRR 0.200→0.778
     ```

     **recall@3 到了 100%** ——对「归属速记时给三个候选」这个用途已经够用。
     含中文材料一栏完全不变，说明改动是外科式的：只动了该动的部分。
     **查询改写（原计划的阶段 3）因此降级为备选**，暂不实现——省掉一整条运行时链路。

     顺序调整的原始理由（仍然成立）：先做索引时翻译——收录纯英文材料时用模型
     生成中文检索关键词写进 frontmatter（`keywords_zh`），查询时不调模型。
     的理由是数量级：查询时改写的调用次数=查询次数，索引时翻译的调用次数=材料数，
     而个人知识库的查询远多于收录；且查询时改写会在每次检索里插一次模型往返，
     而那正是 ADR-0009 要求压到最低的摩擦。
  4. 加向量，做混合检索 + RRF，重测

  跳过第 2 步就失去了判断后续改动是否有效的能力，那是这套流程里最值钱的一步。

  **两条实测结论（2026-09-16，6 篇材料 + 2 条速记）**：

  1. **第一个该调的是分词与停用词，不是模型。** 把中文切成单字 + 二字组会让「所以」「要么」
     这类功能词变成信号，长文档因此虚高。同一条查询：原样排名 5/6 → 丢弃出现在 ≥50% 文档的词
     排名 2/6 → **只用拉丁词项排名 1/6**。上模型改写之前先把这层调对。
  2. **评估集设错会让你去修不该修的东西。** 我给第二条速记设的期望答案是
     `expressjs/compression`，但那篇里 `undici` 0 次、`proxy` 0 次——正确答案根本不在库里。
     基于这个错误样本我一度得出「BM25 比随机还差」，差点据此论证要上模型。
     **建评估样本时必须先确认正确答案确实存在于库中。**
  **跨语言 gap 是真的，已实测（2026-09-16，10 条评估集 / 6 篇材料）**。表述几经反复，以这版为准：

  ```
  中文材料为目标  n=7   recall@1=100%
  英文材料为目标  n=3   recall@1=  0%    排名 [3, 5, 5]
  ```

  材料**语言混杂**：`expressjs/compression` 与 `SSE 规范（WHATWG）` 是纯英文（中文占比 0%），
  其余四篇中文占 34–54%。而查询与**标注**是中文。中文二字组在纯英文文档里零命中，
  BM25 无论怎么调参都跨不过去——这个 gap **只能靠模型解决**：查询改写（阶段 3）或多语言向量（阶段 4）。

  **顺带一条方法论教训**：整体 recall@1=60% 是把两个区间（100% 与 0%）平均出来的，
  掩盖了真正的失效模式。**按目标材料的语言分组看，才发现症结。聚合指标会骗人。**
- **切块**：~~材料按标题层级切~~ **这条规则不可执行，已作废**。实测 6 篇真实材料：
  真正的结构层级每篇都不同（Promise 那篇在 h4，47 个；手撕代码在 h2，11 个；
  SSE 规范 12292 字符里只有 5 个标题）。按 h2 切 → Promise 变成 3 个巨块，最大 39700 字符；
  按 h4 切 → 图解网络一块都切不出来。**固定级别必然在某些材料上失效。**
  切块策略待定，见开放项。标注一条一块这点不变。
  **标注必须与其附着的材料块一同进入检索结果**——问答时要同时拿到「原文说什么」和「本人当时想什么」。
- **图谱**：目标是**找孤岛**（没有任何标注指向、也没发出过标注的材料 = 存了但从没消化的），不是展示。

## 收录链路的实现约束

**收件箱**是 Chrome 的一个书签夹（ADR-0002）：

- 书签文件：`~/Library/Application Support/Google/Chrome/Default/Bookmarks`（JSON）
- 结构：`roots.{bookmark_bar, other, synced}`，文件夹节点含 `type` / `name` / `children`
- 目标文件夹名：`Zhandao待收录`（递归查找，不假设它在哪一层）

正文抽取：

- 默认路径是服务端 fetch + 可读性提取。**不引入无头浏览器**——技术文档多为静态 HTML，为剩下两三成装 Chromium 成本不成比例，且它解决不了最常见的付费墙与登录墙。
- **抽取失败的条目不进 `materials/`。** **材料**按定义是"原始内容"，空正文是死条目：检索不到、出不了题，还虚增库的规模感。
- **抓取要顺序进行、带间隔、带完整浏览器请求头**（`Accept` / `Accept-Language` / `Referer`），
  不要并发扫全部。掘金等站点会在连续请求后返回空壳页——实测 2492 字节、正文 13 字。

- **必须区分临时失败与永久失败**，两者处置不同：

  | 类型 | 例子 | 处置 |
  |---|---|---|
  | **永久** | 付费墙、登录墙、PDF、真正的 JS 渲染页、`no_content`、`not_html`、4xx | 当场手动补正文，或当场划掉 |
  | **临时** | 限流、`timeout`、`network`、5xx、429、`too_short` | 同一次过闸内退避重试；仍失败则降级按永久处理 |

  `too_short` 天生分不清「被限流」和「页面真是空壳」，**默认归为临时**：误重试的代价是多一次请求，
  误判永久的代价是丢掉好内容或白手工粘贴一篇十分钟后能自动抽出的文章。

- **永久失败的条目只有两个出口：当场补正文，或当场划掉。仍然不要建跨会话的「待处理」队列。**
  那种队列会永久增长，正是 ADR-0002 点名的主要失效模式（收件箱堆积）。
  同一次过闸内的退避重试不落盘、不跨会话、随过闸结束消失，因此不构成堆积，不违反本条。
- PDF 第一版不支持，直接当失败处理。攒够几篇再决定是否上解析，现在做是猜需求。

**代理（实测过，别删）**：本机把出网流量走本地代理（`http_proxy=http://127.0.0.1:7897`）。
`curl` 认这个环境变量，**Node 的 fetch（undici）不认** —— 于是需要代理的站点全部
`UND_ERR_CONNECT_TIMEOUT`，10 秒超时后还会被当成临时失败重试 3 次，白烧 30 秒。

实测数据：

```
不带处理                    ✗ 10.5s  UND_ERR_CONNECT_TIMEOUT
NODE_USE_ENV_PROXY=1        ✓ 1.8s   200  361KB
undici EnvHttpProxyAgent    ✓ 1.7s   200  361KB
```

选用**代码内 `setGlobalDispatcher(new EnvHttpProxyAgent())`**，不用环境变量：launchd 定时脚本
（ADR-0004）和测试是另外两条启动路径，环境变量会被忘，而失效是静默的——表现为
「这篇抓不到」，容易误判成网站的问题。`undici` 必须是显式依赖，不能依赖它作为传递依赖存在。

过闸界面：

- **进页面立刻显示书签标题列表**（数据已在本地，零延迟），抓取在后台顺序进行，
  抓好的条目逐个变成可判断状态。**不要做「等全部抓完再显示」**——顺序抓 + 1.5 秒间隔下，
  40 条要等一分钟以上，而白屏是最好的劝退手段，直接违反 ADR-0002 的启动摩擦约束。
- 这样等待被阅读时间吸收：处理第一条要十几秒，足够后面两三条抓完。
- 因此后端必须暴露抓取进度，前端订阅它。
- **抓取到的正文必须立刻落到 `data/.cache/`，不能只留在内存里。** 实测教训：
  原实现把结果放在作业内存并在 30 秒后清理，于是「读完文章再决定」（真实过闸的必然节奏）
  之后点「留下」直接 400。过闸随时可中断这条要求，既要保住**决策**（`processed.jsonl`），
  也要保住**抓取成果**——否则中断一次就得全部重抓，还可能触发限流。
  `.cache/` 正是 ADR-0003 定义的可重建派生物该待的地方。
- **区分「前端没传」和「服务端丢了」。** 缓存缺失不能报成 `Missing or empty markdown`——
  那会让人以为是请求写错了。要给一个明确的、可提示用户重新抓取的错误。
- **逐条结果必须逐条推送。** `onProgress(done, total, url)` 只报进度、不带结果，
  服务端因此在整批跑完前拿不到任何单条结果，`item` 事件只能在最后一起发——
  界面上就是多条同时卡在「抓取中」，「抓好的逐个变成可判断状态」并未实现。
  所以 `extractArticles` 必须提供 `onItem(item)` 回调。
- **SSE 的 `item` 载荷不等于内部的 `ExtractResult`**（正文只传前 300 字，不传全文）。
  前端应当声明自己的 wire 类型并按 `ok` 字段收窄，**不要用 `as any` 绕过判别联合**。

## 功能优先级

1. 收录无摩擦 —— 否则库是空的
2. 标注无摩擦 —— 否则库没价值
3. 出题 —— 否则没人回来读
4. 关联 —— 在写标注时由 agent 触发，不做成独立功能
5. 检索 / 问答 —— 是上面四条的地基，但它本身不是目的

## 命令

目前唯一可跑的是决策文档的校验脚本（来自 grill-with-docs 插件）：

```bash
在 `code/` 根目录：

```bash
npm run dev          # 一条命令拉起前后端（server:3001 / client:5173）
npm test             # 跑 server 全部测试
npm run typecheck    # 两个 workspace 都检查
npm run note -- "一句话"     # 记一条速记（不需要服务在跑，见 ADR-0009）
npm run note -- --list       # 列出待归属的速记
```

跑单个测试文件：`cd server && node --import tsx --test src/quicknotes/append.test.ts`

决策文档的校验脚本（来自 grill-with-docs 插件）：

```bash
S=~/.claude/plugins/cache/claude-code-skills/grill-with-docs/2.9.0/skills/grill-with-docs/scripts
python3 $S/context_md_linter.py CONTEXT.md
python3 $S/adr_scanner.py docs/adr/
python3 $S/glossary_code_consistency.py --context CONTEXT.md --code src/
```

改动 `CONTEXT.md` 结构或新增 ADR 后跑对应那条。第三条在有代码之前会把全部术语误报为 dead glossary。

## 新增 ADR 的门槛

三条全中才写，否则会攒出一堆没人看的文件：**难以回退**、**脱离上下文会让人困惑**、**是真权衡的结果**。

已有两个决定因"容易回退"被判定不够格而未立项：关联必须带理由、题目不留存。它们记在 `CONTEXT.md` 里。

## 开放项

按必须决定的时机排列：

- **调度冷启动**：没有答题记录时，第一批推什么
- **收件箱堆积**的缓解机制（ADR-0002 已点名此为本项目主要失效模式）
- **切块粒度**：块多大、是否重叠。按固定标题层级切已被实测否掉（见技术路线），策略待定；
  注意索引是可丢弃的派生物（ADR-0003），所以推迟这个决定没有迁移成本
- **分词与停用词**：中文单字 + 二字组会让功能词变成信号。实测只用拉丁词项能把一条查询从 5/6 拉到 1/6，
  但那会丢掉全部中文检索能力，不是最终方案
- ~~**评估集**怎么建~~ **已有初版**：`data/eval/retrieval.jsonl`，10 条查询，
  每条的正确答案都用 grep 验证过确实存在于目标材料中（这一步不能省——我曾编过一个
  不存在的答案，据此得出「BM25 比随机还差」的错误结论）。
  指标：recall@1 / recall@3 / MRR，**并且必须按目标材料语言分组看**。
  这份评估集编码的是「什么算对」，属于本人的判断，需本人复核
- **收录时自动生成关键词**：现在靠 `npm run keywords -- --write` 手动回填，
  长期应在**收录**（keep 端点）时就做掉，否则每收一篇英文材料都要记得跑一次脚本
- **关键词近义冗余**：模型偶尔输出「服务器推送」与「服务端推送」这类一字之差的重复项。
  去重只去完全相同的。对 BM25 无害（仅词频略高），暂不处理
- **分词的 CJK 策略**：实测「只要二字组、丢单字」recall@1 从 60% → 70%，但那只是 10 条里差 1 条，
  在噪音范围内。tokenize 要把单字 / 二字组做成可开关，等评估集够大再定
- **embedding 模型选型**：到阶段 4 再查当时现状，不要凭记忆选型号
- ~~**备份策略**~~ **已解决**：`data/` 推送至私有远端 `github.com/Wind-FanCY/Zhandao-data`，异地 + 有版本史。残余风险仅为 GitHub 账号丢失
- **目录页/索引页的处置**（检索收尾后单独 grill）。实测：`xiaolincoding.com/network/` 是专栏
  **介绍页**，不是文章——VitePress 默认 SSR，抽取一点没丢（可见文本 4415 字符，我们抽了 5912），
  是收藏的 URL 层级错了。而该材料正文里含 **41 条指向真文章的子页面链接**，
  所以目录页作为**材料**无价值、作为**收录**源很有价值。
  倾向的方案：过闸时识别并问「要不要把这些链接加进**收件箱**」。
  两个难点：① 识别必然是启发式的（链接密度高 + 正文短 + 标题全是元信息），会误判引用密集的正常文章；
  ② 41 条一次灌进收件箱可能直接把它撑爆，而堆积是 ADR-0002 点名的主要失效模式
- **URL 规范化**：已处理清单按 URL 过滤，追踪参数 / 短链 / 镜像站会被当成新条目（ADR-0008）
- **抓取限流**：掘金等站点会在连续请求后返回空壳页，表现为 `too_short`，与「页面是 JS 渲染」不可区分
- **本机代理未启动**：代理是本机进程（127.0.0.1:7897），它没开时抓取失败会被判为
  `network / transient` 重试三次，与「网站挂了」不可区分。需要一个能区分两者的信号
- **图谱的折叠 / 过滤策略**（ADR-0001 已预告需要）
