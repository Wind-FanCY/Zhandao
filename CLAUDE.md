# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目状态

决策记录：`CONTEXT.md`（术语表）与 `docs/adr/`。**数量一直在增长，不在本文件里复述**——
跑下面「命令」一节的两个 linter 就是权威答案。
**所有实测数字、基线、被推翻的结论都在 `docs/findings.md`**；本文件只留约束与「为什么」。
看到任何百分比先去那边确认它的样本量。

已完成：**收录**全链路（书签**收件箱** → 抓取抽取 → 过闸落盘）、**速记**记录入口、
BM25 检索与评估脚本、索引时翻译、**归属**链路（速记 → 标注，含查询提炼）、
**索引页**展开（`npm run expand`）、**推送**链路（`SessionStart` hook + 阅读视图 + 留档/划掉）、
**预练**链路（练题提取 + 作答揭晓 + 会/不会 自评）。
三份可浏览列表都已就位：**材料**（阅读 / 归属 / 预练三处，按**索引页**分组折叠）、
**标注**（`GET /api/annotations`，在阅读列表里内联显示）。

闭环现状：

```
收录 → 标注 → 出题 → 推送 → 答题 → 暴露弱点 → 回去补标注
 ✅     ✅     ❌      ✅     ❌
```

**真正的瓶颈是标注太少，而它只能靠使用产生。** 库里**材料**几十篇、**标注**个位数，
于是绝大多数材料是孤岛（存了但从没消化）。这不是指标坏了，是真相。

**剩下两环都卡在同一件事上**：一道**题目**由恰好一条**标注**生成、该**标注**就是标准答案
（CONTEXT.md），所以标注只有几条时根本出不了题；没有**题目**就没有**答题记录**，
而**答题记录**是 ADR-0004 定的调度唯一信号源。
**推送这一环正是为此先做的**——它推的是「去读这篇」而不是「来答这道题」，
目的就是把标注攒起来。所以现在最该做的事不是写代码，是**用它**。

**出题的开工条件是一个真实时刻，不是一个标注数**（2026-09-20 定）：
**本人第一次看着自己写过的一条标注、想不起来当初在说什么。**
理由有两层。① **数量是错的度量，时间才是**——出题唯一独有的功能是揭示
「**我自己的**理解衰减了」（与**预练**的分界见 ADR-0011），而衰减要时间；
库里最老的**标注**只有 4 天时出这几道题会全对，而全对**不产生任何调度信号**。
② **挑一个「攒到 15 条就做」式的常数，与 ADR-0004 正面冲突**——那份 ADR 的立场是
「依据本人自己的表现，不要通用间隔」，推送池因此刻意不设定时器、不挑冷却常数；
给开工时机挑常数等于把刚赶出门的东西从后门放回来。
**这个判据的失效模式是「永远注意不到那个时刻」**，对策见下一节末尾那条（标注必须可浏览）。

**预练**（2026-09-19 实现，见 ADR-0011）是闭环**之外**的一条旁路：
它用**材料**里现成的问题逼出「我原来想错了」，而那一句正是一条**标注**。
所以它目前是最有可能把标注攒起来的入口——但它自己不在闭环里，也不改**孤岛**判据。
实测七篇材料提出 187 道**练题**（三篇面试题材料 96 道），原料管够；
**现在缺的仍然不是功能，是用它**。

目录布局：`Zhandao/` 是容器（非仓库），其下 `code/`（本仓库）与 `data/`（材料与标注，独立私有仓库）平级——见 ADR-0005。

容器目录里有**两条软链接**，都指向 `code/` 下的真身（同一 inode），这样从 `Zhandao/` 开会话也能拿到全套配置：

```
Zhandao/CLAUDE.md  ->  code/CLAUDE.md      加载得到约束
Zhandao/.claude    ->  code/.claude        加载得到 SessionStart hook 与 permissions
```

内容只有一份、受 `code/` 版本控制。**链接本身不受版本控制**，坏了就重建：

```bash
cd ~/Desktop/wind-Fancy/Zhandao
ln -sf code/CLAUDE.md CLAUDE.md
ln -sfn code/.claude .claude      # -n：别跟进已存在的目录链接里去建嵌套链接
```

`.claude` 那条是 2026-09-18 加的，实测触发点：从 `Zhandao/` 起会话时 hook 不触发
——项目级设置按**启动目录**找，而容器不是 git 仓库，所以项目根就是 `Zhandao/` 本身。
**软链整个目录而不是单个文件**：将来在 `code/.claude/` 里加 `agents/` 或 `commands/` 会自动跟上。
hook 命令里写的是**绝对** `cd`，所以从哪个目录起都能执行。

本文件里的相对路径（如 `docs/findings.md`）都相对 `code/`；在容器目录开会话时是 `code/docs/findings.md`。

## 术语表要维护 `_Code_` 行

`CONTEXT.md` 里每个术语都带一行 `_Code_`，记它在代码里的名字（`材料 → material / materials/ / writeMaterial`）。
**原因**：术语是中文、标识符是英文，两者之间本来没有任何机械可验证的联系——
`glossary_code_consistency.py` 会把所有术语报成 dead glossary，等于永久无效。

由此得到的具体好处是**能记录刻意不存在的东西**。ADR-0001 禁止引入 topic / tag 实体，
这条约束原先只靠"有人读了本文件"来维持；现在 `CONTEXT.md` 里写着可执行的检查：

```bash
grep -rniE "\btag\b|\btopic\b|\bcategory\b|知识点" server/src | grep -v test   # 应无输出
```

**重构改了标识符名字，就要同步这一行。** 忘了也没关系——一致性脚本会把它报成 dead，那就是提醒。

动手写任何代码前先读这两处。下面列出的约束全部来自它们，且**大多与常规做法相反**——照直觉写会违反其中好几条。

## 这是什么

一个本地运行、按需启动的个人知识库，主题是全栈开发与 agent 开发。核心是一个闭环：

```
收录 → 标注 → 出题 → 推送 → 答题 → 暴露弱点 → 回去补标注
        检索贯穿全程
```

价值全部集中在**标注**上，不在**材料**上。材料是网上搜得到的东西，存一份不产生增量；标注是本人写的、搜不到的东西。**任何让收录或标注变麻烦的设计都在削弱这个项目。**

## 界面必须支持浏览，不能只有搜索

召回有三种强度：主动搜索最弱（**你想不起来的东西不会去搜**），系统推送居中，被迫调用最强。
本项目的核心失效模式是"没有读路径 → 库会死"，所以界面必须提供**可浏览的列表**——
哪怕只是标题列表。认出来（recognition）比想起来（recall）容易得多，这是搜索框给不了的。

推论：任何"只提供搜索入口"的设计（纯 CLI、只有一个搜索框的首页）都在削弱读路径，不要采用。

**另一条推论，实测得来**：**做决定所需的控件必须在默认视图里，只有「想深入看看」的内容才藏在展开后面。**
第一次真实过闸时六个标题一个都没改——不是懒，是标题输入框被放在「展开」面板里，本人根本没看见。
原设计正好反了：**必须设置**的标题被藏起来，而**可选**的正文预览反而是展开的理由。
可选步骤一旦需要额外点击，在批量流程里就等于不存在。

**第三条推论，同样是实测得来**：**一个功能的入口挂在哪张列表上，那张列表的筛选语义
必须和这个功能的需要一致。** **预练**的入口一度挂在「阅读」的列表里，而那张列表是**推送池**、
按定义排除已消化的**材料**；于是预练刚逼出一条**标注**，那篇材料就从入口消失了。
**动手前问一句**：这个功能会不会改变条目在这张列表里的去留？会的话，改变之后还进得来吗？

**标注也必须有浏览路径，不能只能从它挂靠的材料点进去。** 实测倒挂（2026-09-20）：
**材料**有三条浏览路径（阅读 / 归属的全量列表 / 预练），**标注**有**零条**——
界面上标注只在「打开某篇材料之后的详情里」渲染，也没有任何列出标注的端点。
而本文件第一条原则写的是「**价值全部集中在标注上，不在材料上**」：
**库里唯一值钱的那层，恰好是唯一没有浏览路径的那层。**
这条还是「出题等一个真实时刻」那个判据的**配套件**——那个时刻的定义是
「看着自己写过的一条标注想不起来」，看不见就永远不会发生，判据会退化成无限期搁置。
**不要用检索代替它**：检索是三档召回里最弱的那档（「你想不起来的东西不会去搜」），
而「标注进不进检索索引」那条开放项另有要求（必须作为带对照的独立改动去测），别搭车。

**材料列表按 `from`（索引页）分组折叠，但分组只活在呈现层。** 实测 49 篇**材料**里
41 篇共用一个**索引页**，不分组的话列表 84% 是一堵同质的墙、标题还高度相似；
而**排序换不掉这一点**（换个顺序还是同一堵墙），筛选框也早就有了。
ADR-0010 的 Consequences 原本就点过名：「缓解在呈现层：候选里显示 `from`」。
**分组不得渗进数据模型**——`from` 不是分类、不是**关联**、不影响**孤岛**判定，这是 ADR-0010 的硬线。

顺带一条 git 配置：两个仓库都设了 `core.quotepath false`。默认配置会把中文文件名转义成
`\343\200\220…`，而本项目所有文件名都是中文，不设这个 `git log` / `git status` 基本不可读。

## 术语是有约束力的

`CONTEXT.md` 定义的术语不是同义词（数量见 linter 输出，别在这里记）。最容易搞错的六处：

- **材料**是导入的原始内容，**标注**是本人写下的理解。两者不是一回事，不要统称"笔记"。
- **收件箱**里的条目**还不是材料**——它还没过"值得留吗"这道闸。
- **题目**不留存，**答题记录**留存，且挂在**标注**上而非题目上。
- **索引页**不是**材料**（无内容，按定义不够格）；它的作用是展开出多份**材料**，
  并被记在那些**材料**的 `from` 字段里。见 ADR-0010。
- **材料不出题。** 一道**题目**由恰好一条**标注**生成、**标注**即其标准答案，
  所以没有**标注**的**材料**（包括**留档**过的）一道题都出不了。
  这不是**留档**的特性，是所有**材料**都如此——出题的原料是**标注**，不是**材料**。
- **归属**不是**关联**。归属是「这条**标注**挂在哪份**材料**上」，恰好一条、必然存在；
  关联是那条**标注**额外指向其他内容的边，零到多条。**标注不是「两份材料之间的线」**，
  而是锚在一份**材料**上、可向外发线的节点。这两者已经被混淆过一次（本人与我各一次），
  所以**归属**的 `_Avoid_` 里就列着「关联」。

## 硬约束

每条都有 ADR 支撑。要改动的话先读对应 ADR，别直接推翻。

| 约束 | 出处 |
|---|---|
| 不要引入「知识点」/ topic / tag 实体或表。分类从关联图算出，不预先存 | ADR-0001 |
| 关联必须由一条**标注**承载。不要创建没有理由的"相关"边 | ADR-0001 |
| 不要做 bookmarklet 或浏览器扩展。收录是异步的，走浏览器书签收件箱 | ADR-0002 |
| 不要部署到公网。本地、按需启动 | ADR-0002 |
| Markdown 文件是真相源。SQLite 里的一切必须可从文件重建，且可随时删库 | ADR-0003 |
| 不要写数据库迁移脚本。schema 变了就删库重建 | ADR-0003 |
| 复习调度依据答题表现。不要实现艾宾浩斯 / SM-2 之类的通用间隔算法 | ADR-0004 |
| 定时触发用 launchd，不要起常驻进程 | ADR-0004 |
| **不要写 Chrome 的书签文件**（有 checksum、在云端同步、被 Chrome 进程持有）。已处理条目靠 `data/` 里的清单过滤 | ADR-0008 |
| **练题记录不进复习调度，预练不清孤岛、不进推送池。** 不要把**练题**和**题目**、**练题记录**和**答题记录**合并 | ADR-0011 |

## 技术路线

方向已定，尚未落地。

- **语言**：TypeScript。**前后端分离**：前端 Vite + React，后端 Express（ADR-0007）。
  一体化框架（Next.js / SvelteKit）已被明确否决，不要提议迁移——先读 ADR-0007。
  必须用单条 `npm run dev` 同时拉起前后端，否则违反 ADR-0002 的启动摩擦约束。
- **模型调用**：`server/src/model/` 是**唯一**知道用哪家服务商的地方。
  现在是 **DeepSeek**（`deepseek-flash`，走 OpenAI 兼容接口 `https://api.deepseek.com`）。
  **为什么只有一家却单独关一个目录**——这条已被实战检验过一次：原本选的是 Claude，
  但学校组织账号的 API key 未绑定 workspace，调任何接口都返回 400
  （要求 `anthropic-workspace-id` 头），而本人无权新建 key。换成 DeepSeek 的
  **实际改动范围就是这一个文件**，调用方一行未动。
  **DeepSeek 与 Anthropic 的四处实质差异**（前三处依官方文档，第四处是实测撞出来的；
  别照搬 OpenAI 的通用写法）：
  ① OpenAI 兼容接口 + `baseURL`；② **没有严格 JSON Schema**，只有
  `response_format: {type:"json_object"}`，schema 校验得自己用 zod 兜；
  ③ 文档明示「API 可能偶尔返回空内容」——已按此重试一次，两次都空就返回空数组，
  不让关键词缺失拖垮整条收录流程。
  **④ 还有第四处，是实测撞出来的、文档没写的：`deepseek-flash` 是推理模型，
  `max_tokens` 是「推理 + 正文」的总预算，而推理会先把它花光。** 表现极像 ③——
  「模型没返回内容」——但抬 `max_tokens` 治不了。炸点不是输入长度，是
  **「在长输入上逐条枚举并判断」这类任务**（同一篇 41246 字符的材料，要 10 个关键词
  1024 预算就够，要枚举 52 个标题判断则 8192 全被推理吃光、正文 0 字节）。
  **对策是缩输入而不是加预算**，见「预练链路的实现约束」。数字在 `docs/findings.md`。
  **刻意没有造 `interface ModelProvider`**：现在有**三个**调用点
  （`keywords.ts` 关键词提取、`distill-query.ts` 查询提炼、`extract-drills.ts` 练题提取），
  形状仍然看不出共同点——分别吃「材料正文」「一句速记」「一组候选行」，
  吐「字符串数组」「一个字符串」「{行号, 问题} 数组」，
  连失败语义都不同（第一个静默返空数组，后两个抛错）。
  原定「等调用点长到三个以上、能看出共同形状时再抽」——**数量门槛到了，形状门槛没到**。
  判断留在开放项。
  凭证放 `code/.env` 的 `DEEPSEEK_API_KEY`（`.env` 已被 gitignore，而本仓库是公开的）。
- **Agent**：**自己写 agent 循环**。不要引入 DeepSeek Harness、LangChain 之类的 harness/框架——本项目的目的之一就是学 agent 怎么工作，把循环抽象掉就学不到了。模型本身随意，DeepSeek 可用。
- **RAG 分四阶段推进，每阶段对基线量化**。`npm run eval` 是长期存在的尺子。
  **所有基线数字、消融结果、被推翻的结论都在 `docs/findings.md`，不在本文件**——
  数字会随样本量翻面，写进约束文件必然过期。
  1. BM25 检索 → 生成，拿到基线 —— **已完成**
  2. 建评估集 —— **已完成**：`data/eval/retrieval.jsonl`。指标是 recall@1 / recall@3 / MRR
     （**不是** recall@5），**必须按目标材料语言、且按查询来源 `origin` 分组看**。
  3. 索引时翻译 —— **已完成，有效，但没有关上跨语言 gap**。收录纯英文材料时用模型生成中文
     检索关键词写进 frontmatter 的 `keywords_zh`，一并进索引；`npm run keywords -- --write` 回填。
     曾以为它关上了 gap，那是 n=3 的过拟合（findings 有原表）。
     **因此查询改写仍在计划内**，它曾被误判为「已被索引时翻译取代」。
     **先做索引时翻译、把查询改写放后面，这个顺序的理由仍然成立**：查询时改写的调用次数
     = 查询次数，索引时翻译 = 材料数，而个人知识库的查询远多于收录；且查询时改写会在
     每次检索里插一次模型往返，而那正是 ADR-0009 要求压到最低的摩擦。
     **但这个理由只在「每次检索都改写」这个形态下成立，已有反例**：**归属**路径上调用次数
     = **速记**数（且永久缓存），且归属不是当场动作、不受 ADR-0009 的摩擦约束——
     所以那里单独实现了查询提炼（见「归属链路的实现约束」）。
     **那不算阶段 3**，别拿本条理由去否掉它，也别拿它去把阶段 3 标成完成。
  4. 加向量，做混合检索 + RRF，重测

  跳过第 2 步就失去了判断后续改动是否有效的能力，那是这套流程里最值钱的一步。
- **当前唯一真实的检索缺陷是跨语言 gap**：含中文材料 recall@3=100%，纯英文 50%（n=12，2026-09-26 复核）。
  **别在本文件里追这个百分比**——它随样本量翻面，权威值跑 `npm run eval` 现场看。
  BM25 调参跨不过去（两代基线反复确认），**只能靠模型**——查询改写或多语言向量。
- **动检索之前先读 `docs/findings.md` 的「被推翻的结论」。** 已经有五条结论被后续测量推翻，
  其中三条是我基于小样本给出的建议。**在当前规模下，检索实验的结论不可外推。**
- **`tokenize` 的单字 / 二字组开关保留，默认值不动**——丢单字的收益已被证明是小库假象。
- **改动检索后必须按语言和来源分组复核，不看聚合数字。** 聚合会把 100% 和 0% 平均成 50%。
- **评估集的增删改需本人复核。** 它编码的是「什么算对」，属于本人的判断，不是代码的测试固件
  ——所以它住在数据仓库（`data/eval/retrieval.jsonl`）而不是 `server/src`。
  **新增样本必须先 grep 确认正确答案确实存在于目标材料中**，这一步不能省（findings 有教训）。
- **切块**：~~材料按标题层级切~~ **这条规则不可执行，已作废**。实测 6 篇真实材料：
  真正的结构层级每篇都不同（Promise 那篇在 h4，47 个；手撕代码在 h2，11 个；
  SSE 规范 12292 字符里只有 5 个标题）。按 h2 切 → Promise 变成 3 个巨块，最大 39700 字符；
  按 h4 切 → 图解网络一块都切不出来。**固定级别必然在某些材料上失效。**
  切块策略待定，见开放项。标注一条一块这点不变。
  **标注必须与其附着的材料块一同进入检索结果**——问答时要同时拿到「原文说什么」和「本人当时想什么」。
- **图谱**：目标是**找孤岛**，不是展示。**孤岛的定义已于 2026-09-18 修订**（见 `CONTEXT.md`）：
  旧定义是「没有标注指向的材料」，新定义加了「且未经**留档**」——因为读完并判断过就是消化了，
  旧定义把「读过并判断不写」误算成孤岛，而当时还没有推送→阅读这条链路、「读过」无处可记。

## 怎么读评估指标（每次看到数字前先读这里）

`npm run eval` 的唯一原始测量是**正确答案排在第几位**。N 条查询 = N 个排名数字，
三个指标都只是在把这 N 个数字压缩成 1 个——压缩必然丢信息，各自丢的不一样。

| 指标 | 定义 | 丢掉什么 | 对应的使用方式 |
|---|---|---|---|
| `recall@1` | 排名 = 1 的比例 | 把「第 2 名」和「第 100 名」当成一样糟 | 「我只看第一条」 |
| `recall@3` | 排名 ≤ 3 的比例 | 抹平前三名内部的差别 | 「给我几个候选我自己挑」 |
| `MRR` | 1/排名 的平均 | 无阈值，连续 | 看趋势：**没跨过门槛但在往上走** |

**三个必须一起看。** recall 是阶梯函数（每条查询只有对/不对两态），MRR 是连续的：
实测出现过 `recall@1` 纹丝不动而 `MRR` 动了的情况，也出现过 `recall@1` 上升而 `@3` 不动
——后者意味着「要么第一名要么完全不出现」，是退步而非改进。

### 噪音地板：**一条查询翻面值多少个百分点，就是你的分辨率**

`recall@1` 只能以 `1/N` 为步长变化。**当前 n=31，一条查询 = 3.2 个百分点，
所以差距小于约 6% 的结论一律不当真**（n=10 时这个门槛是 20%）。

**换了评估集规模就要重算这个门槛，并且不能拿新旧基线直接比大小**——
语料规模也会变（6 篇 → 46 篇），随机猜中的概率从 1/6 变成 1/46，任务难度根本不同。

**有对照组不变，是判断「外科式改动」还是「过拟合评估集」的关键。**
已据此拒绝和推翻过的结论共五条，全部记在 `docs/findings.md`，别重复走。

### 它不测什么

- **不测答案好不好**：只测「能否把已知答案排前面」。材料本身是垃圾也照样能 100%。
- **不测真实查询分布** —— 这是最大的偏差来源。那些查询绝大多数是**我猜**本人会问什么。
  这条预警已经兑现过一次：同一把尺子、同一个索引，评估集上 100%、真实**速记**上 0%,
  **差别只在查询是谁写的**。因此评估集有 `origin` 字段（`guessed` / `real_note`）并按它分组——
  **让这道偏差每次跑 eval 都可见，而不是每隔几周重新发现一次。**
  `kind`（`short` / `oral`）也保留在评估集里，供样本够大时重测句式假设。
- **不测「库里没有答案」这种情况**：评估集每条都保证有答案。
  而实测已出现过没有宿主**材料**的**速记**（undici / 代理那条），真实使用中会经常发生。

> **因此这份评估集的正确用法是：判断「改动 A 比改动 B 好吗」（相对比较，对偏差不敏感）。
> 不能用它宣称「检索准确率有 80%」（绝对断言，偏差直接进结论）。**

## 外部数据必须校验，不能用断言（zod 的位置）

TypeScript 的类型编译完就被擦掉，运行时不存在。所以 `JSON.parse(x) as Foo`
**不是检查、是断言**——你告诉编译器「相信我」，它就真的信了。

判断标准：**校验只该出现在边界上。** 自己代码内部 TS 类型够用；边界是数据从外面进来、
你无法信任它的地方。本项目有**五**处边界，按可信度排：

| 边界 | 来自 | 当前做法 |
|---|---|---|
| DeepSeek 返回的 JSON | 模型 | **zod**（形状嵌套 + 无严格 schema，最不可信） |
| Chrome 书签文件 | 另一个程序 | 手写 `isBookmarkNode`（私有格式，无兼容承诺） |
| 材料的 frontmatter | 本人 + 模型 | 手写 `typeof` 收窄 |
| 评估集 JSONL | 本人 | 坏行跳过 |
| **HTTP 请求的 query / body** | 浏览器与 curl | 手写 `isRecord` / `typeof` 收窄 |

**HTTP 请求这一行是 2026-09-20 补的，而它是最经常被外部输入触达的一处。**
表里缺了它的直接后果：`app.ts` 里那几处一直用 `as` 断言处理，从没被当成边界看。
其中一处是真会出错的——`req.query.jobId as string | undefined`，而 Express 在
`?jobId=a&jobId=b` 时给的是**数组**，`if (!jobId)` 对数组恒为假，于是带着数组往下走。
**实测得到的两条**：① `req.body` 的危险场景不是「JSON 不是对象」（那种被
`express.json()` 的 strict 模式挡在外面、返回 HTML 错误页，你的代码跑不到），
而是**请求没有 `Content-Type`**——此时 `req.body` 是 `undefined`，解构当场抛。
② 因此这类回归测试**必须断言错误文案而不只是状态码**，否则会因为错误的原因通过
（第一版就是这么写的，探针一查才发现那个 400 来自 body parser）。

**已知的不一致**：同一件事用了三种写法。界线是「形状嵌套且来源最不可信的用 zod，
扁平的手写 `typeof`」——这是权宜，不是原则。要统一的话统一到 zod。

**今天修过三次同一类问题**，写法不同、本质一样（用断言代替检查）：
`as any`（客户端访问判别联合）→ `as (T)[]`（filter 后去 null）→ `as string`（读 frontmatter
字段，而 YAML 里 `id: 123` 会解析成数字，断言拦不住）。
**grep `as any` 查不到后两种**——审查时要连 `as ` 一起看。

## 收录链路的实现约束

**收件箱**是 Chrome 的一个书签夹（ADR-0002）：

- 书签文件：`~/Library/Application Support/Google/Chrome/Default/Bookmarks`（JSON）
- 结构：`roots.{bookmark_bar, other, synced}`，文件夹节点含 `type` / `name` / `children`
- 目标文件夹名：`Zhandao待收录`（递归查找，不假设它在哪一层）

正文抽取：

- 默认路径是服务端 fetch + 可读性提取。**不引入无头浏览器**——技术文档多为静态 HTML，为剩下两三成装 Chromium 成本不成比例，且它解决不了最常见的付费墙与登录墙。
- **抽取失败的条目不进 `materials/`。** **材料**按定义是"原始内容"，空正文是死条目：检索不到、出不了题，还虚增库的规模感。
- **抓取要顺序进行、带间隔、带完整浏览器请求头**（`Accept` / `Accept-Language` / `Referer`），
  不要并发扫全部。掘金等站点会在连续请求后返回空壳页——实测 2492 字节、正文 13 字。

- **必须区分临时失败与永久失败**，两者处置不同：

  | 类型 | 例子 | 处置 |
  |---|---|---|
  | **永久** | 付费墙、登录墙、PDF、真正的 JS 渲染页、`no_content`、`not_html`、4xx | 当场手动补正文，或当场划掉 |
  | **临时** | 限流、`timeout`、`network`、5xx、429、`too_short` | 同一次过闸内退避重试；仍失败则降级按永久处理 |

  `too_short` 天生分不清「被限流」和「页面真是空壳」，**默认归为临时**：误重试的代价是多一次请求，
  误判永久的代价是丢掉好内容或白手工粘贴一篇十分钟后能自动抽出的文章。

- **永久失败的条目只有两个出口：当场补正文，或当场划掉。仍然不要建跨会话的「待处理」队列。**
  那种队列会永久增长，正是 ADR-0002 点名的主要失效模式（收件箱堆积）。
  同一次过闸内的退避重试不落盘、不跨会话、随过闸结束消失，因此不构成堆积，不违反本条。
- **Readability 会把代码块连壳一起吞掉**，所以 `extract.ts` 在交给它之前必须调
  `unwrapCodeWrappers()` 把 `<pre>` 从外壳里拎出来。判据**刻意不按 class 名匹配**
  （比如 `div[class*="language-"]`），那等于给某个站点生成器打补丁；判据是
  「这个 div 的文本基本就是这段代码本身」，站点无关。
  **改动它必须复核掘金那篇 Promise 面试题的围栏数不变**（244）——那是零退步的基准。
  隔离过程与实测数字见 `docs/findings.md`。
- **GitHub README 少围栏是另一个原因，不要顺手一起改**：它的 `<pre>` 没有 `<code>` 子节点，
  turndown 的围栏规则不触发，代码以纯文本落盘。**内容没丢**（`app.use` 4 次、`require(` 11 次都在），
  BM25 照样检索得到，只是人读起来没有代码块的形。属于格式债，不是内容债。
- PDF 第一版不支持，直接当失败处理。攒够几篇再决定是否上解析，现在做是猜需求。

**代理**：本机出网走本地代理（`http_proxy=http://127.0.0.1:7897`），而 **Node 的 fetch（undici）
不读这个环境变量**（`curl` 读）。必须用**代码内 `setGlobalDispatcher(new EnvHttpProxyAgent())`**，
**不要靠环境变量**：launchd 定时脚本（ADR-0004）和测试是另外两条启动路径，环境变量会被忘，
而失效是静默的——表现为「这篇抓不到」，容易误判成网站挂了。
`undici` 必须是显式依赖，不能依赖它作为传递依赖存在。实测数据见 `docs/findings.md`。

**调本地服务要加 `curl --noproxy '*'`**，否则 `localhost:3001` 会被代理拦下返回 502。

过闸界面：

- **进页面立刻显示书签标题列表**（数据已在本地，零延迟），抓取在后台顺序进行，
  抓好的条目逐个变成可判断状态。**不要做「等全部抓完再显示」**——顺序抓 + 1.5 秒间隔下，
  40 条要等一分钟以上，而白屏是最好的劝退手段，直接违反 ADR-0002 的启动摩擦约束。
- 这样等待被阅读时间吸收：处理第一条要十几秒，足够后面两三条抓完。
- 因此后端必须暴露抓取进度，前端订阅它。
- **抓取到的正文必须立刻落到 `data/.cache/`，不能只留在内存里。** 实测教训：
  原实现把结果放在作业内存并在 30 秒后清理，于是「读完文章再决定」（真实过闸的必然节奏）
  之后点「留下」直接 400。过闸随时可中断这条要求，既要保住**决策**（`processed.jsonl`），
  也要保住**抓取成果**——否则中断一次就得全部重抓，还可能触发限流。
  `.cache/` 正是 ADR-0003 定义的可重建派生物该待的地方。
- **区分「前端没传」和「服务端丢了」。** 缓存缺失不能报成 `Missing or empty markdown`——
  那会让人以为是请求写错了。要给一个明确的、可提示用户重新抓取的错误。
- **逐条结果必须逐条推送。** `onProgress(done, total, url)` 只报进度、不带结果，
  服务端因此在整批跑完前拿不到任何单条结果，`item` 事件只能在最后一起发——
  界面上就是多条同时卡在「抓取中」，「抓好的逐个变成可判断状态」并未实现。
  所以 `extractArticles` 必须提供 `onItem(item)` 回调。
- **SSE 的 `item` 载荷不等于内部的 `ExtractResult`**（正文只传前 300 字，不传全文）。
  前端应当声明自己的 wire 类型并按 `ok` 字段收窄，**不要用 `as any` 绕过判别联合**。

## 归属链路的实现约束

**归属**是把一条**速记**挂到某份**材料**上、使它成为一条**标注**的动作（ADR-0009 的第二段）。
整条链路在形状上与**收录**完全平行，直接照抄已跑通的那一套：

| | 收录 | 归属 |
|---|---|---|
| 候选集 | Chrome 书签夹 | `quicknotes.jsonl` |
| 决策日志 | `processed.jsonl` | `quicknotes-processed.jsonl` |
| 产物目录 | `materials/` | `annotations/` |
| 两个终态 | `kept` / `dropped` | `attached` / `dropped` |

- **决策日志是唯一记「已处理」的地方，标注 frontmatter 不记 `quicknote` 字段。**
  那个 id 除了记账没有其他用途（文本已经在文件里），记两处就会分叉。
  与 ADR-0010 的分界一致：`from` 进 frontmatter 是因为「哪个**索引页**展开了我」无处可查。
  **已知代价**：日志丢了无法从 `annotations/` 反推哪些速记已归属（文本可被编辑过，没法按文本匹配）。
- **候选宿主给 3 个。** ~~BM25 直出，不调模型~~ **这条已被推翻**（2026-09-17）：
  当初的依据是「索引时翻译之后 recall@3=100%」，而那个 100% 是 **n=3 英文样本的过拟合**
  （见 `docs/findings.md`）。第一次真实**归属**就把正确宿主排在第 5 名。
- **不要拿速记原文当检索查询，先用模型把它提炼成查询。** 实测依据（49 篇库）：

  ```
  速记原文（长中文）                          → 连前 12 都进不去
  「undici 不读 http_proxy 代理」（8 个字）    → 第 1 名
  ```

  **所以问题不是跨语言、不是库太小，是原文里的填充词把信号淹了。**
  本人明确选择用模型而不是机械丢弃高 df 词项——**已知代价**：引入第二个模型调用点，
  以及「模型抽歪了」这个新失效模式，而它的收益**目前无法测量**（评估集 31 条全是短问句，
  真实速记只有 1 条）。下面三条约束都是给这个代价上的保险。
- **提炼出的查询必须可见、可编辑、改了能重搜，且在默认视图里。** 它是这一轮的「标题输入框」：
  决定候选的控件藏起来就等于不存在（过闸那条实测教训）。
  作用是把静默失效变成可见失效——看到查询框里写着「代理 系统功能」就知道模型抽错了。
- **提炼结果缓存进 `.cache/`，键是速记 id，永久有效。** 速记原文不可变
  （`quicknotes.jsonl` 追加式），所以提炼结果是原文的**纯函数**，不需要任何失效逻辑。
  `.cache/` 正是 ADR-0003 给可重建派生物留的地方，删了就重新调一次模型。
- **提炼可以并发，抓取不可以。** 抓取必须顺序 + 1.5 秒间隔，理由是**站点限流**；模型 API 没有这个理由。
  因此**阻塞首屏是可接受的**，不必再建一条 SSE——「白屏是最好的劝退手段」那条约束的实测依据是
  「40 条 × 1.5 秒 = 一分多钟」，而这里是并发 + 永久缓存，**规模上不是同一件事，不要照抄那条约束**。
- **提炼失败时不要退回用原文搜。** DeepSeek 会偶尔返回空内容（官方文档明写），所以失败是必然路径。
  退回原文会产出**三个看起来正常、实际全错**的候选，而你不知道提炼失败了。
  正确处置是**直接展开全部材料列表 + 明确标注「自动提炼失败」**，把不确定性交还给人
  （ADR-0010 「零误判，因为是人点的」）。
- **这不是 RAG 阶段 3。** 阶段 3 的验收标准是「跨语言 gap 关上了吗」，而这次改动完全不碰那个。
  路线图状态一个字不动。
- **「都不合适」必须是一等公民动作，不设自动阈值。** BM25 总会返回三个候选，
  即使全不相关。评估集规模（当前 n=31）远不足以校准一个阈值——该不该选由本人判断，与 ADR-0010 「零误判，因为是人点的」同理。
- 选了「都不合适」就提示去**收录**一篇（CONTEXT.md 已定）。提示只能是文字指引
  （`Ctrl+D` 进 `Zhandao待收录`）——**不得写 Chrome 书签文件**（ADR-0008）。
- **那句话可编辑，标注文件的正文是真相源**；`quicknotes.jsonl` 那行不动。
  理由见 CONTEXT.md 的 Flagged ambiguities。
- **textarea（预填速记原文）、三个候选、归属 / 丢弃 按钮全部在默认视图里。**
  这是过闸界面那条实测教训的直接应用：可选步骤一旦需要额外点击就等于不存在。
- **必须有一份可浏览的完整材料列表，不能只给检索出的三个候选。** 这条是实测逼出来的，
  不是设计洁癖：**第一次真实归属时，三个候选全错，正确宿主排第 5**（原表见 `docs/findings.md`）。
  原设计的第四个选项直接是「都不合适 → 去收录一篇」，于是它会**催人重新收录一篇库里已有的材料**。
  这撞的是「界面必须支持浏览，不能只有搜索」那条约束——归属界面当时恰恰只有检索结果。
  现在「都不合适」展开的是全部**材料**（带标题筛选），`GET /api/materials` 供这个用途，
  **顺序稳定是它的功能而非细节**：列表每次刷新都重排，人就没法靠位置认出条目。
  「确实没有 → 去收录」退到列表下方作为最后一档。
- **那次候选排错的根因后来查明是库太小**（6 篇），不是分词、不是模型、不是界面：
  同一条**速记**在 46 篇库里正确宿主排**第 1**，什么都没改。
  当时我一度归因于「速记是长句」，那是错的——短问句对照同样进不了前三。
  **所以这条兜底列表不是权宜之计也不是永久方案**：它在材料只有几十篇时够用，
  上到几百篇时浏览会失效，那时要靠的是检索本身变好。
- **归属没有改变评估集基线**（实测复核：recall@1=80% / @3=100% / MRR=0.883，与落盘前一致），
  这是上一条「v1 不把标注加进索引」实际生效的证据。
- **v1 不把标注加进检索索引**（2026-09-16 定）。三条理由，第三条是决定性的：
  ① 现有唯一的检索消费者是「找宿主**材料**」，而宿主按定义只能是材料，
  标注混进候选只会产生**点不了的候选**；② 索引是可丢弃派生物（ADR-0003），
  推迟零成本；③ **会把尺子改坏**——同时改变被检索集合与基线，
  之后的数字不再与记录的基线可比，而这份评估集的正确用法**只有**
  「改动 A 比 改动 B 好吗」。索引标注该作为**带对照的独立改动**去测。
  技术路线里「标注必须与其附着的材料块一同进入检索结果」讲的是**问答阶段**，
  而问答依赖切块粒度（仍在开放项）——所以它不是 v1 的债。

## 推送链路的实现约束

**已实现。** 决定与理由见 `CONTEXT.md` 的 Flagged ambiguities 与术语**留档** / **孤岛**。
入口：`SessionStart` hook 自动跑（见下），手动是 `npm run push`，读与处置在「阅读」标签。

- **冷启动期推的是一篇未消化的材料（读），不是题目（考）。** 出题不是目的，「让本人回来」才是。
  2 条**标注**只能出 2 道题，第三天就背熟了——而 ADR-0004 自己写着「间隔重复本质是背诵工具……属工具错配」。
- **推送池顺序：孤岛优先，其次留档时间最早。不设定时器、不挑冷却常数。**
  「留档最久」是**排序**不是**到期日**——没有东西会到期，它只是在前面的池子空了之后成为下一个。
  ADR-0004 的立场是「依据本人自己的表现，不要通用间隔」，而这个方案里没有任何常数。
- **池子里没有合适的就不推。**「今天没有」是合法输出。硬推一条昨天刚**留档**的材料，
  等于给一个已经答过的问题——而那正是 A 方案唯一的失效模式（一路点留档会让孤岛池迅速见底）。
- **推送只给一篇，不报总数、不给进度条。** 每天提醒本人欠着几十篇会让人不想打开它，
  而「任何让收录或标注变麻烦的设计都在削弱这个项目」。
- **只新增「留档」这一种记录。** 「有标注」从 `annotations/` 的 `material` 字段可查、
  「删掉了」文件不在且 `processed.jsonl` 有 `dropped` 记录——**只有「读过但没标注」无处可查**。
  与「决策日志是唯一记已处理的地方」同一个原则：每个事实只存一处。
- **触发器是 Claude Code 的 `SessionStart` hook，不是 launchd**（2026-09-18 改）。
  launchd 在 `~/Desktop` 下跑不通：TCC 拒绝后台 agent 读该目录的文件内容、也拒绝执行其中的脚本
  （实测见 `docs/findings.md`）。搬仓库与给 `/bin/bash` 开完全磁盘访问都被否决。
  **这正是 ADR-0004 在 Considered Options 里否决过的「启动时结算」**，理由是
  「没有外部触发，等于承认该功能作废」。为什么仍然接受：那份 ADR 想的是
  「打开 Zhandao 主服务时结算」——需要本人**先决定去看知识库**；而 Claude Code 的会话
  是为**别的事**打开的（写代码、问东西），推送是**不请自来**地找到本人的。
  **相对于知识库，这个触发是外部的。**
  **已知代价**：只在这个仓库目录开会话时触发；在别处开 Claude Code 不会。
- **推送要轮转，同一篇不能天天推**（2026-09-20→23 实测后改）。原实现直接取池首，
  而**孤岛没有任何重复保护**（同日去重那条只管**留档**），于是池首会每天推同一行
  直到本人处理它——`手撕代码篇` 连着推了四天。这撞的正是本节下一条自己的理由：
  「同一行反复出现是噪音，**而噪音会让人开始忽略它**」，当初只应用在同一天之内。
  本人原话：**「我觉得每天推不一样的篇章说不定我看到想看的就点进去了」**——
  那正是本文件开篇那条「认出来比想起来容易」，而卡住不动的那行只给一次认出的机会。
  **规则**：在**每个层级内部**（孤岛 / 留档各自）按「上次被推的时间」升序重排，
  从没推过的算最久；**层级之间的顺序不动**，孤岛仍然优先于留档。平手按原来的 `since`。
  **三条性质要守住**：① **不引入任何常数**——「最久没推过的排前面」和「留档最早的排前面」
  是同一种排序，不是到期日（ADR-0004）；② **不需要「一轮」这个概念**，全推一遍之后
  最久没推的那篇自己回到队首，天然循环；③ **上次推送时间绝不能当全局主键**，
  那会让**留档**插到**孤岛**前面，推翻 ADR-0004 定的优先级。
- **「今天该读哪篇」是一个事实，不是一次重算。** 三个入口（hook / `GET /api/push/today` /
  `npm run push`）**必须给出同一篇**：今天的日志里有记录就返回那一篇，没有才计算。
  **这条是被 bug 逼出来的**（2026-09-26）：轮转让 `pickForPush` 读推送日志来排序，
  而它自己的执行又会往那个日志里写——于是 hook 07:43 推了 `2.1`、记进日志，
  之后网页再问同一个函数，看到 `2.1` 刚推过、排到队尾，**返回了 `2.2`**。
  本人直接看到两处说法不一致。
  **一般教训，比这个 bug 值钱**：**给一个查询函数引入历史依赖，会悄悄打破所有调用方
  对幂等的假设**。改之前 `pickForPush` 是纯函数式的「按池子算出队首」，谁问都一样；
  改之后它变成「算出**下一篇**」，而三个调用方里有两个问的其实是「**今天是哪篇**」。
  **同一个函数被当成两种语义用，而历史依赖让这两种语义分了岔。**
  **配套的测试教训**：轮转当时有 5 条测试，全绿——因为它们测的是
  「推过 A 之后给 B」，**那恰好就是 bug 的行为**。单元测试断言了错误的语义为正确，
  因为它们只覆盖了「决定下一篇」，没有覆盖「同一天问两次答案要一样」。
  **引入历史依赖时，必须专门测「问两次，答案不变」。**
- **推送日志 `data/pushes.jsonl` 是不可再生数据，不能住 `.cache/`。**
  轮转要知道「推过哪些」，所以它是**实现的一部分**而不是仪表。
  与 `materials-read.jsonl` 有一处相反：那个取**最早**（排序看存量多老），这个取**最新**
  （「上次什么时候推的」是当前状态）。
  **只在本人这次真的看到推送时才追加一条**——即 `--once-per-day` 的闸放行的那次。
  不带该参数的手动 `npm run push` 是**查询**，连敲五次不该轮转掉五篇；
  被闸拦下的也不写（本人已经看过了）。`--dry-run` 不影响写不写，它只管发不发系统通知。
  按天去重的状态 `<dataDir>/.cache/push-last-shown` 不变，它可丢，与这条日志是两回事。
- **必须按天去重**（`--once-per-day`）。hook 每开一个会话就触发一次，一天可能好几次，
  同一行反复出现是噪音，而噪音会让人开始忽略它——与「不报总数」同一个理由。
  状态在 `<dataDir>/.cache/push-last-shown`，**可丢**（丢了最坏就是多提示一次）。
  **默认行为刻意不去重**：手动敲 `npm run push` 就该永远给答案。
- **hook 里用 `--dry-run`（不发系统通知）**：正文已经进对话了，再弹一条通知是重复噪音。
- **呈现给本人的那一条是 JSON 的 `systemMessage`，界面上显示为「SessionStart:resume says: …」。**
  **绝对不要同时设 `suppressOutput: true`**——一度那么设，结果连 `systemMessage` 一起被压掉，
  看起来像「`SessionStart` 不渲染 `systemMessage`」，白绕了一轮。
  （未单独隔离验证，但那行 `says` 的内容正是 `systemMessage` 的值，
  而它出现的那次恰好是去掉 `suppressOutput` 的那次。）
- **纯 stdout 不行**：hook 成功时界面几乎不显示（Claude Code 的设计是静默成功不打扰，
  只有报错/超时才显眼）。第一版就是这么写的，hook 确实跑了、输出也进了模型上下文，
  但本人完全没看到——**那是可见性问题，不是没触发**。
- **`additionalContext` 只用来让模型知道今天推了什么，不要在里面指示模型复述。**
  `systemMessage` 已经显示过了，模型再说一遍是重复噪音——与「不报总数」同一个理由。
- **`claude -c`（resume）照样触发**，不是可见性问题的原因——系统行有证据。
- **「阅读」左侧列全部材料，不是只列推送池。** 池子里的排前面、标「孤岛」/「留档」，
  已有**标注**的排后面、标「已标注」。**不要退回只列池子**：那样一写**标注**，材料就从界面消失、
  正文再也打不开，直接撞「界面必须支持浏览」那条硬约束（2026-09-20 实测，本人原话
  「我标注过的不再显示了」）。**但不要加总数计数器**——列表可见是一回事，
  每天报「你还欠 46 篇」是另一回事，后者被本节上面那条明令禁止。
- 脚本本身仍按 ADR-0004：读数据、算一篇、退出——**不起常驻进程、不碰模型、不联网**
  （`fetch` 与模型调用在 `push.ts` / `pool.ts` 里各 0 处，已 grep 确认）。

## 预练链路的实现约束

**已实现**（2026-09-19，见 ADR-0011 与 `CONTEXT.md` 的**预练** / **练题** / **练题记录**）。
**预练**是考前工具，与闭环的**出题 / 答题**是两条链路、共享零个数据结构。
入口：「阅读」视图里那篇**材料**上的「预练」按钮。

- **它不卡在标注上。** 本文件曾记着「面试前预练要等**标注**攒够」，**那是把优先级写成了依赖**。
  原料是**材料**正文里现成的问题，一条**标注**都不需要（数字见 `docs/findings.md`）。
- **绝对不要把材料正文全文喂给模型。** 只喂**候选行**（所有标题行 + 所有整行加粗行，各带行号），
  模型只返回 `{line, question}`，**锚点由我们按行号查表取原文**。
  两个理由，第二个比第一个重要：
  ① 全文喂法在大材料上**必然失败**——`deepseek-flash` 是推理模型，推理会吃光 `max_tokens`
  （41246 字符的材料：8192 预算全进推理，正文 0 字节），抬预算治不了，且失败是非确定性的。
  ② **模型只能从我们给出的行里挑，连编一道没有锚点的题的物理可能性都没有**——
  ADR-0011 的「提取而非生成」不变量因此从**事后检查**变成**由构造保证**。
  原先那套「模型转录 anchor + `locateAnchor` 三级模糊匹配」已删，别复活它。
- **但这不是纯机械提取，别退回去。** 机械只负责**枚举候选位置**（含 `## 防抖函数`、
  `##### 精灵图` 这种不带问号的标题）；**哪些值得练、问题怎么问仍由模型判断**。
  纯机械提取实测在最该被预练的那三篇**材料**上只出 3 道题（模型出 96 道），
  而绕过去的唯一办法是写站点特定正则——与 `unwrapCodeWrappers` 明令禁止的那类错误同形。
  两组原表与复现命令都在 `docs/findings.md`。
- **成本形状与索引时翻译 / 查询提炼逐字相同**：调用次数 = **材料数**（不是查询次数），
  **材料**正文不可改 → 提取结果是正文的**纯函数** → 永久缓存，删了重跑。
- **练题清单住 `.cache/`（可丢弃派生物），练题记录住 `data/`（不可再生、追加式）。** 别放一起。
- **必须先写下答案才能揭晓原文；长度不限。** 不作答的「我会」只是一种感觉，而感觉正是要被测的那个东西
  （本人原话：「可是我不做答我就不知道自己会不会」）。但强制完整作答会把 45 道题变成数小时打字，
  撞上 ADR-0009 的摩擦约束。**不设长度阈值**——速扫时一行、想练表达时一段，由本人按题决定，
  与「都不合适不设自动阈值」同理。
- **作答文本不留存，只留会/不会那一位。** 作答是对原文的复述，属**标注** `_Avoid_` 点名的「笔记」，
  留存它会在 `data/` 里堆起一层没有增量的文字。
- **不调模型批改。** 判断自由文本与原文的语义等价是这堆里最难的模型任务，判错直接污染**预练**唯一
  产出的那条记录；它省下的只是「你自己扫一眼原文」。**这是纯增量**，将来要加只是换掉会/不会那一位的来源。
- **一轮不必练完一篇。** 记录是按**练题**的，练到哪算哪；下一轮默认只出上次标了「不会」的那些
  （一个集合差，无间隔、无常数）。
- **预练有自己的标签页，入口不能只挂在「阅读」里。** 这条是实际使用逼出来的（2026-09-20）：
  「阅读」列表的数据源是 `GET /api/materials/pool`（**推送池** = **孤岛** + **留档**），
  它**按定义排除已消化的材料**——于是**预练**刚逼出一条**标注**，那篇**材料**当场离开池子、
  从列表消失，**再也回不去继续练**。本人原话：「我标注过的不再显示了，我找不到继续预练的路子」。
  **两张列表的筛选语义是相反的**：推送要「还没消化的」，预练要「全部」，不能共用。
  「阅读」里那个按钮保留，但它是捷径不是入口。跨材料组卷仍留作开放项。
- **界面同样受「做决定所需的控件必须在默认视图里」约束**：作答框、揭晓、会/不会 三者都不得藏在展开面板后面。

## 功能优先级

1. 收录无摩擦 —— 否则库是空的
2. 标注无摩擦 —— 否则库没价值
3. 出题 —— 否则没人回来读
4. 关联 —— 在写标注时由 agent 触发，不做成独立功能
   - **归属界面的 v1 暂不做关联**（2026-09-16 定）。理由不是工期：库里**标注**是 0 条，
     agent 能提议的目标只有那 6 篇**材料**，而材料间的字面相关**恰好是 BM25 已经覆盖、
     ADR-0001 说不该记成关联的那一类**。等**标注**攒到十几条、目标能是标注了再开。
     frontmatter 里 `targets: []` 是合法终态（CONTEXT.md：零到多条），**不是欠账**。
5. 检索 / 问答 —— 是上面四条的地基，但它本身不是目的

## 命令

在 `code/` 根目录：

```bash
npm run dev          # 一条命令拉起前后端（server:3001 / client:5173），两端都带热重载
npm test             # 跑 server 全部测试
npm run typecheck    # 两个 workspace 都检查
npm run note -- "一句话"     # 记一条速记（不需要服务在跑，见 ADR-0009）
npm run note -- --list       # 列出待归属的速记
npm run push                 # 今天该读哪篇（加 --dry-run 不发通知）
npm run drills               # 每篇材料提了多少道练题（只读，不调模型）
npm run drills -- --warm     # 把还没提取的都提一遍（花钱：调用次数 = 未缓存的材料数）
npm run drills -- --force    # 重提全部（某篇提歪了时用——缓存永久有效，不会自己失效）
npm run expand -- <索引页URL> # 把索引页展开成材料（加 --dry-run 只列链接）
```

跑单个测试文件：`cd server && node --import tsx --test src/quicknotes/append.test.ts`

决策文档的校验脚本（来自 grill-with-docs 插件）：

```bash
S=~/.claude/plugins/cache/claude-code-skills/grill-with-docs/2.9.0/skills/grill-with-docs/scripts
python3 $S/context_md_linter.py CONTEXT.md
python3 $S/adr_scanner.py docs/adr/
python3 $S/glossary_code_consistency.py --context CONTEXT.md --code src/
```

改动 `CONTEXT.md` 结构或新增 ADR 后跑对应那条。第三条在有代码之前会把全部术语误报为 dead glossary。

## 新增 ADR 的门槛

三条全中才写，否则会攒出一堆没人看的文件：**难以回退**、**脱离上下文会让人困惑**、**是真权衡的结果**。

已有两个决定因"容易回退"被判定不够格而未立项：关联必须带理由、题目不留存。它们记在 `CONTEXT.md` 里。

## 开放项

按必须决定的时机排列。**已解决的不留在这里**——理由该在 ADR、`CONTEXT.md` 或 `docs/findings.md` 里，
留一堆划掉的条目只会让这份清单越来越难读。

- **跨材料组卷**：预练 v1 按**材料**进入；「给我 20 道混合的模拟面试」要先知道哪些题该混，
  而那依赖题库长到几百道。**题库这个闸已经开了**（2026-09-26：49/49 篇全部提取，共 502 道）。
  **但别把「练题记录只有 2 条」读成「预练没人用、所以别增强它」**——
  ADR-0011 写着**预练是考前工具，不承担「让库不死」那份责任**，
  **低频是设计预期不是缺陷**。真实状况是「还没有一次考前冲刺发生过」，
  而组卷的价值恰好只在那个时刻显现。这两种读法指向相反的动作，别搞混
  （2026-09-26 我就先读错了一次，本人纠正）。
- **模型批改预练作答**：会/不会现在是本人自评，没有外部校准（防线只有「必须先写下来」）。
  加批改是纯增量，但**要先有使用数据说明自评确实不可信**，否则是凭想象加一个最难的模型任务
- **`interface ModelProvider` 要不要抽**：三个调用点到齐了（关键词提取 / 查询提炼 / 练题提取），
  **数量门槛到了，形状门槛没到**——三者的入参、返回、失败语义两两不同（见「技术路线」）。
  倾向再等；**但别再拿「只有一个调用点」当理由**，那句已过期
- **孤岛数会长期难看**：几十篇材料、个位数标注。ADR-0010 已预告批量**收录**会让它暴增。
  需要决定图谱/健康度界面怎么呈现它，才不会每次打开都像在骂自己。
  **按索引页分组折叠已经缓解了一半**（2026-09-20）：49 行折成 9 行之后，
  看到的是「8 样东西 + 一套网络教程」而不是「你欠着 45 篇」——**同样的事实，不同的形状**。
  剩下的一半是那个还不存在的图谱界面
- **跨语言 gap**：含中文材料 recall@3=100%、纯英文 50%（n=12），BM25 调参跨不过去（见 findings）。
  只能靠模型——查询改写或多语言向量。**库里英文材料 5 篇**（2026-09-26 复核；
  此处一度写着「只有 2 篇」，那个数字过期了）。5 篇仍然少，但已经不是「在 2 个样本上调」——
  **重新判断的时候到了，别再拿旧数字当拦路理由**
- **收件箱堆积**的缓解机制（ADR-0002 已点名此为本项目主要失效模式）
- **切块粒度**：块多大、是否重叠。按固定标题层级切已被实测否掉（见 findings），策略待定；
  注意索引是可丢弃的派生物（ADR-0003），所以推迟这个决定没有迁移成本
- **标注进不进检索索引**：v1 刻意没进（理由见「归属链路的实现约束」）。
  要做就必须作为**带对照的独立改动**去测，别搭在别的改动上
- **收录时自动生成关键词**：现在靠 `npm run keywords -- --write` 手动回填，
  长期应在**收录**（keep 端点）时就做掉，否则每收一篇英文材料都要记得跑一次脚本
- **关键词近义冗余**：模型偶尔输出「服务器推送」与「服务端推送」这类一字之差的重复项。
  去重只去完全相同的。对 BM25 无害（仅词频略高），暂不处理
- **embedding 模型选型**：到阶段 4 再查当时现状，不要凭记忆选型号
- **URL 规范化**：已处理清单按 URL 过滤，追踪参数 / 短链 / 镜像站会被当成新条目（ADR-0008）。
  已经有一个实例：索引页那条记录带着 `#质量如何` 这个 fragment
- **抓取失败的条目不进已处理清单**，所以重跑 `npm run expand` 会重试它们。
  这是刻意的（记成已处理等于静默丢内容），但坏链接会被无限重试
- **抓取限流与 JS 渲染不可区分**：都表现为 `too_short`
- **本机代理未启动与网站挂了不可区分**：都是 `network / transient` 重试三次。需要一个能区分两者的信号
- **图谱的折叠 / 过滤策略**（ADR-0001 已预告需要）
- **`CLAUDE.md` 与 `docs/findings.md` 的边界要守住**：**约束进前者、证据进后者**，
  这是唯一要守的规则。**不设行数上限**（2026-09-20 改）：当初胀到 577 行拆分，
  病因是混放实测档案而不是长度本身；拆完之后文件又长了，那是因为链路从 2 条长到 5 条、
  每条都有一节不可省的约束。**别把约束挪进 `docs/`**——本文件唯一的价值就是被自动加载，
  挪出去等于让它们失效。看到它变长，先问「新增的是约束还是证据」，是证据就搬去 findings
